package practice1;

public class Student {
	String batch;
	Student(){};
	Student(String batch){
		this.batch = batch;
	}
	 void setBatch(String batch){
		this.batch = batch;
	}
	 String setbatch(String batch) {
		 return batch;
	 }
}
